# hugo.toml file

baseURL = "https://dorensa.com/"
languageCode = "en-us"
title = "Hello"
theme = "HelloWhite"

# Ensure "page" is NOT disabled here
disableKinds = ["taxonomy", "section", "RSS", "sitemap", "robotsTXT", "404", "home"] 

[taxonomies]
  category = ""
  tag = ""
